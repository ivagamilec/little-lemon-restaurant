import React from "react";

const Nav = () => {
    return (
        <nav>
            <a href="/">
            <img src={} alt="logo" />
            </a>
        </nav>
    );
};

export default Nav;